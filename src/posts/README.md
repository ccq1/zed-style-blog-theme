# 📝 如何添加新文章

## 快速开始

1. 复制 `_template.md` 文件
2. 重命名为你的文章文件名（例如：`my-new-post.md`）
3. 修改顶部的 YAML frontmatter
4. 在 `---` 分隔符后面编写文章内容
5. 刷新页面，新文章会自动显示

## Frontmatter 字段说明

### 必填字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | string | 唯一标识符 | `'1'`, `'2'` |
| `slug` | string | URL路径（小写+连字符） | `'hello-world'` |
| `title` | string | 文章标题 | `'我的第一篇文章'` |
| `date` | string | 发布日期 | `'Nov 24, 2025'` |
| `author` | string | 作者名 | `'张三'` |
| `category` | string | 分类 | `'Tutorials'`, `'Projects'`, `'Thoughts'`, `'Snippets'` |

### 可选字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `summary` | string | 摘要（推荐60-120字符） | `'这是一篇关于...'` |
| `authorAvatar` | string | 作者头像URL | `'https://...'` |
| `readTime` | string | 阅读时长 | `'5 min'` |
| `tags` | array | 标签列表 | `['React', 'TypeScript']` |
| `coverImage` | string | 封面图片URL | `'https://...'` |

## 分类说明

支持4种分类，每种有不同的颜色：

- `Tutorials` - 蓝色 💙（教程类）
- `Projects` - 绿色 💚（项目类）
- `Thoughts` - 紫色 💜（想法类）
- `Snippets` - 橙色 🧡（代码片段类）

## 显示顺序

文章按 **date 字段** 自动排序，最新的文章会显示在最前面。

## 注意事项

1. ⚠️ 文件名以 `_` 开头的文件会被忽略（如 `_template.md`）
2. ⚠️ 确保 `id` 和 `slug` 在所有文章中是唯一的
3. ⚠️ `date` 格式要能被 `new Date()` 正确解析
4. 💡 `coverImage` 推荐使用 [Unsplash](https://unsplash.com) 的图片
5. 💡 `authorAvatar` 可以使用 GitHub Avatars

